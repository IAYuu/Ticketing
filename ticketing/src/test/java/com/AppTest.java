package com;

import static org.junit.Assert.assertTrue;

import com.utils.database.DataBase;
import com.utils.database.SqlCmd;
import com.utils.database.Station;

import org.junit.Test;

// enum CMDTYPE {
//     INSERT, DELETE, UPDATE, SEARCH, NONE
// };

/**
 * Unit test for simple App.
 */
public class AppTest 
{
    public static void main(String[] args) throws Exception {

    // DataBase st = new Station();
    // SqlCmd sc = new SqlCmd(com.utils.database.SqlCmd.CMDTYPE.INSERT, "stations");
    // sc.setProperty("id", "4");
    // sc.setProperty("start", "'a'");
    // sc.setProperty("reach", "'b'");
    // sc.setProperty("price", "20");
    // sc.setProperty("start_time", "'12:30'");
    // sc.setProperty("reach_time", "'11:30'");
    // st.execute(sc);
    } 
}
